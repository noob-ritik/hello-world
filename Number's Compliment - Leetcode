 int findComplement(int num) {
        if(num==0)
        return 1;
      int ans=0;
      int i=0;
      while(num>0)
      {
        int b=num%2;
        if(b==0)
          ans+=pow(2,i);
        num/=2;
        i++;
      }
      return ans;
        
    }
